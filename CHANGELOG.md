# 변경 기록

이 프로젝트의 주요 변경 사항을 기록합니다. 형식은
[Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르며, 버전은
[Semantic Versioning](https://semver.org/lang/ko/)을 기준으로 관리합니다.

## [Unreleased]

### 추가

- 이벤트 이름 단일 목록(`APP_EVENT_NAMES`)과 타입 안전한 `EventBus`를 추가했습니다.
- 저장 데이터의 스키마 기본값·정규화·중복 제거·콘텐츠 너비 범위 검사를
  `src/vault/schema.ts`로 통합했습니다.
- 여러 채널 요청을 최대 4개까지 병렬 처리하는 순서 보장 동시성 유틸리티를
  추가했습니다.
- 필터 규칙, URL 라우팅, 저장 스키마, 이벤트 실행, 네트워크 오류, 저장소,
  마이그레이션, 동시성 동작을 검증하는 Node 테스트 25개를 추가했습니다.
- Node.js 22.13에서 `npm run check`를 수행하는 GitHub Actions CI를
  추가했습니다.
- Windows, macOS, Linux에서 동일하게 동작하는 릴리즈 스크립트를 추가했습니다.

### 변경

- 실행 중 들어온 사용자 이벤트를 버리지 않고 순서대로 처리하도록 중앙 이벤트
  큐를 도입했습니다.
- `StepRunner`를 단계 간 순차 실행, 단계 내부 병렬 실행, 실패 즉시 중단,
  병렬 형제 작업 정리 후 오류 전파, `finally` 저장 보장 구조로 단순화했습니다.
- URL 판별과 ID 추출을 취약한 문자열/정규식 조합에서 `URL`,
  `URLSearchParams`, 정확한 경로 세그먼트 기반으로 교체했습니다.
- 게시글 요청에 8초 타임아웃, HTTP 상태 확인, 페이지 URL 순환 감지, 최대
  10페이지 제한, 요청 간 중복 제거, 동시 로더 참조 카운팅을 적용했습니다.
- 홈 시리즈의 다중 채널 로딩을 최대 4개 동시 요청으로 최적화하고, 일부 채널
  실패가 전체 피드를 중단하지 않도록 격리했습니다.
- 홈/게시글/스크랩 시리즈 유형과 홈 채널별 커서·소진 상태를 세션 단위로
  저장하도록 변경했습니다. 기존 다중 채널 홈 시리즈도 자동 마이그레이션합니다.
- 게시글 목록과 설정 상태를 불변 방식으로 갱신하고, 동일 상태의 불필요한
  저장을 생략하도록 개선했습니다.
- 모달, 버튼, 키보드 이벤트, 리사이즈 핸들 초기화를 멱등적으로 만들고
  접근성 레이블·키보드 조작·포커스 트랩·포커스 복원을 보강했습니다.
- 마우스 리사이즈 갱신을 animation frame 단위로 제한하고, 모션 감소 설정에서
  로더 애니메이션을 중지하도록 변경했습니다.
- 개발 빌드를 기본 1회 실행으로 바꾸고 `dev:watch`를 분리했으며, 개발/배포
  유저스크립트의 CDN 의존성 버전을 고정했습니다.
- 타입 검사 범위를 강화하고 테스트 전용 TypeScript 설정, 포맷 검사, 전체
  `check` 명령을 추가했습니다.
- 수동 릴리즈는 `main`과 원격의 완전 동기화 및 전체 검증 후 태그를 만들고,
  태그 기반 워크플로우가 다시 검증한 뒤 재시도 가능한 GitHub Release를
  생성하도록 통합했습니다.
- 런타임 패키지와 CDN 버전을 정확히 고정하고 Node/npm 요구사항, LF 줄바꿈
  정책을 명시했습니다.

### 수정

- 제목 필터만 설정하면 모든 게시글이 사라지던 문제를 수정했습니다.
- 하이브리드 목록에서 제목을 찾지 못하거나 빈 필터 토큰이 전체 목록을
  차단하던 문제를 수정했습니다.
- 서로 다른 게시글 ID를 부분 문자열로 비교해 현재 위치나 시리즈 중복을 잘못
  판정하던 문제를 수정했습니다.
- 홈 시리즈가 다음 글을 불러오지 못하고 멈추는 문제와 구독 확인 후 모달
  오버레이가 남는 문제를 수정했습니다.
- 일반/스크랩 시리즈 끝에 홈 구독 채널 글이 섞이거나, 채널별 Swiper 비활성
  설정 때문에 시리즈 탐색이 막히던 문제를 수정했습니다.
- 동적으로 추가된 입력란에서 방향키를 누를 때 게시글이 이동하던 문제를
  수정했습니다.
- 시리즈 목록의 첫 항목 삭제, 활성 인덱스 유실, 짧은 목록의 잘못된 구간 계산,
  중복 DOM 구독으로 인한 반복 실행을 수정했습니다.
- 빈 목록이나 범위를 벗어난 인덱스에서 잘못된 URL로 이동하던 문제를
  수정했습니다.
- 손상된 `localStorage` JSON, 잘못된 인덱스, 누락된 설정 필드가 초기화를
  중단하던 문제를 수정했습니다.
- 여러 탭이 동시에 열릴 때 최근 세션 캐시 인덱스가 덮어써질 가능성을
  `lastAccess` 재탐색과 삭제 유예로 줄였습니다.
- 필터 모달 저장 시 기존 `disableSwiper` 설정이 유실되던 문제를 수정했습니다.
- 중복 로딩 표시기와 전역 CSS 클래스 충돌 가능성을 제거했습니다.
- 페이징 중 후속 요청이 실패하면 앞 페이지에서 수집한 스크랩 결과까지
  버리던 문제와 같은 빈 결과를 즉시 중복 요청하던 문제를 수정했습니다.
- 다중값 쿼리 파라미터가 마지막 값 하나로 합쳐지던 문제를 수정했습니다.
- 수동 릴리즈에서 성공한 Git 명령의 반환값을 잘못 처리해 태그를 즉시
  삭제하던 문제와, 기존 태그 생성 실패 시 그 태그를 삭제할 수 있던 문제를
  수정했습니다.
- 개발 번들에서 Toastify를 CDN과 번들 양쪽에 중복 포함하던 문제를
  수정했습니다.
- 간접 개발 의존성의 공개 보안 취약점을 해소해 `npm audit` 결과를 0건으로
  만들었습니다.

### 제거

- 사용되지 않던 `jquery-ui-dist`, `@types/jqueryui`, 오래된
  `@types/swiper`, Prettier ESLint 플러그인, CSS 모듈 TypeScript 플러그인 등
  직접 의존성을 제거했습니다.
- 사용되지 않던 Store/Vault 재내보내기와 타입 유틸리티 파일을 제거했습니다.
- POSIX 셸 문법에 의존하던 npm 스크립트와 존재하지 않는 `main.js` 선언을
  제거했습니다.

[Unreleased]: https://github.com/Roharui/ArcaFeed/compare/v2.5.0...HEAD
